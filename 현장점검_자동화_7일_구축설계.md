# 현장 점검표 자동 집계·주간보고 시스템 — 7일 실행 설계안

> 시나리오: 화학공장 유틸리티동 설비 20대, 작업자 5명, 매일 점검. **데이터는 전부 합성.** 예산 0원, 구글 무료 계정.
> 설계: Fable 5.1 (2026-09-24)

---

## 1. 전체 구조도

```
[스마트폰 구글폼] ──제출──▶ [구글시트: 원본응답 탭]
                                   │  (폼 제출 트리거)
                                   ▼
                        [Apps Script: onFormSubmit]
                           ├─ 기준값읽기()  ◀── [기준값 탭] (비개발자가 수정)
                           ├─ 이상치판정()  ──▶ [이상이력 탭] 기록
                           ├─ 메일알림()    ──▶ 담당자 Gmail (이상 시에만)
                           └─ 일일집계갱신() ──▶ [일일집계 탭]
                                                     │
[시간 트리거: 매주 월 07:00] ──▶ 주간PDF생성()       │
                           ├─ 주간요약갱신() ◀───────┘
                           ├─ 구글독스 템플릿 복사 → 자리표시자 치환
                           ├─ PDF 변환 → [드라이브: 주간보고 폴더] 저장
                           └─ 관리자 메일 첨부 발송
```

---

## 2. 구글폼 설계

폼 제목: `유틸리티동 일일 설비 점검표`
설명란: "설비 1대당 1회 제출. 숫자는 게이지 표시값 그대로 입력."

| 순서 | 문항 | 타입 | 필수 | 선택지 / 유효성 | 도움말 |
|---|---|---|---|---|---|
| 1 | 점검자 | 드롭다운 | ✔ | 김민수, 이서연, 박지훈, 최유진, 정도현 | 본인 이름 선택 |
| 2 | 설비 | 드롭다운 | ✔ | `AC-01`~`AC-05`(공기압축기), `PU-01`~`PU-08`(펌프), `TK-01`~`TK-04`(탱크), `BL-01`~`BL-03`(송풍기) — 20개 | 설비 명판의 태그번호 |
| 3 | 압축기 토출 압력(bar) | 단답 → 응답확인: 숫자, 0~15 | ✔ | | 소수점 1자리. 압축기 아니면 0 |
| 4 | 오일 레벨 | 객관식 | ✔ | 정상 / 보충필요 | 사이트글라스 기준 |
| 5 | 펌프 진동·소음 | 객관식 | ✔ | 정상 / 주의 / 이상 | 평소와 다른 소리면 주의 |
| 6 | 베어링 온도(℃) | 단답 → 숫자, 0~150 | ✔ | | 비접촉 온도계 측정값 |
| 7 | 탱크 액위(%) | 단답 → 숫자, 0~100 | ✔ | | 레벨게이지 눈금 |
| 8 | 배관 누설 | 객관식 | ✔ | 없음 / 있음 | 플랜지·밸브 주변 확인 |
| 9 | 밸브 잠금 상태 | 객관식 | ✔ | 정상 / 해제됨 | 잠금장치(LOTO) 확인 |
| 10 | 안전 커버·방호 | 객관식 | ✔ | 정상 / 파손 | 커플링 커버 포함 |
| 11 | 윤활 급유 | 객관식 | ✔ | 완료 / 미실시 | 오늘 급유 여부 |
| 12 | 특이사항 | 장문 | ✗ | | 없으면 비워두세요 |

설정: "응답 → 스프레드시트 연결" 후 응답 탭 이름을 `원본응답`으로 변경. "설정 → 이메일 주소 수집"은 **끔**(개인정보 최소화).

---

## 3. 구글시트 구조

| 탭 | 컬럼 헤더(A열부터 실제 문자열) | 채우는 주체 |
|---|---|---|
| `원본응답` | `타임스탬프` `점검자` `설비` `압축기 토출 압력(bar)` `오일 레벨` `펌프 진동·소음` `베어링 온도(℃)` `탱크 액위(%)` `배관 누설` `밸브 잠금 상태` `안전 커버·방호` `윤활 급유` `특이사항` `판정결과` `이상항목수` | 폼 자동 + 스크립트(N,O열) |
| `기준값` | `항목명` `원본컬럼명` `판정유형` `기준값` `이상값목록` `심각도` `담당자이메일` `사용여부` | 사람이 편집 |
| `설비목록` | `설비태그` `설비명` `구역` `담당자` `담당자이메일` | 사람이 편집 |
| `일일집계` | `점검일` `제출건수` `점검설비수` `미점검설비수` `이상건수` `이상설비목록` `최다이상항목` | 스크립트 |
| `이상이력` | `발생시각` `점검일` `설비` `점검자` `항목명` `측정값` `기준` `심각도` `알림발송` `조치상태` `조치내용` | 스크립트(A~I) + 사람(J,K) |
| `주간요약` | `주차` `시작일` `종료일` `총제출` `이상건수` `이상률(%)` `TOP3설비` `TOP3항목` `미조치건수` `PDF링크` | 스크립트 |
| `설정` | `키` `값` — 행 예시: `관리자이메일`, `템플릿문서ID`, `PDF폴더ID`, `알림활성화`(TRUE/FALSE) | 사람이 편집 |

---

## 4. 임계값 관리 (기준값 탭 초기 데이터 — 그대로 붙여넣기)

| 항목명 | 원본컬럼명 | 판정유형 | 기준값 | 이상값목록 | 심각도 | 담당자이메일 | 사용여부 |
|---|---|---|---|---|---|---|---|
| 토출압력 | 압축기 토출 압력(bar) | 초과 | 7.0 | | 상 | mech@example.com | TRUE |
| 오일레벨 | 오일 레벨 | 값일치 | | 보충필요 | 중 | mech@example.com | TRUE |
| 진동소음 | 펌프 진동·소음 | 값일치 | | 주의,이상 | 상 | mech@example.com | TRUE |
| 베어링온도 | 베어링 온도(℃) | 초과 | 70 | | 상 | mech@example.com | TRUE |
| 탱크액위 | 탱크 액위(%) | 미만 | 20 | | 중 | ops@example.com | TRUE |
| 배관누설 | 배관 누설 | 값일치 | | 있음 | 상 | ops@example.com | TRUE |
| 밸브잠금 | 밸브 잠금 상태 | 값일치 | | 해제됨 | 상 | safety@example.com | TRUE |
| 안전커버 | 안전 커버·방호 | 값일치 | | 파손 | 상 | safety@example.com | TRUE |
| 윤활급유 | 윤활 급유 | 값일치 | | 미실시 | 하 | mech@example.com | TRUE |

**원칙:** 스크립트는 `원본컬럼명`으로 응답 열을 찾고 `판정유형`(초과/미만/값일치)으로 비교한다. 기준을 6.5로 바꾸거나 행을 추가하면 코드 수정 없이 즉시 반영. `사용여부=FALSE`면 판정 제외.
→ 면접 시연의 핵심: **"기준값 셀 수정 → 폼 재제출 → 판정 바뀜"**

---

## 5. Apps Script 함수 목록

| 함수 | 역할 | 입력 → 출력 | 호출 시점 |
|---|---|---|---|
| `onFormSubmit(e)` | 진입점. 아래 순서대로 호출 | e.namedValues → 없음 | 폼 제출 트리거 |
| `기준값읽기()` | `기준값` 탭을 객체 배열로 | → `[{항목명, 컬럼명, 유형, 기준, 이상값, 심각도, 메일}]` | 매 판정 전 |
| `이상치판정(응답, 기준목록)` | 항목별 비교 | 응답 → 이상 목록 | onFormSubmit |
| `이상이력기록(응답, 이상목록)` | `이상이력` append + N,O열 기입 | | 이상 1건 이상 시 |
| `메일알림(응답, 이상목록)` | 담당자별 묶어 1통 발송 | → 발송 수 | 이상 시, 알림활성화=TRUE |
| `일일집계갱신(날짜)` | 해당 날짜 행 재계산 | | onFormSubmit 끝 |
| `주간요약갱신(시작일)` | 7일 범위 집계 → `주간요약` 1행 | | 주간PDF생성 내부 |
| `주간PDF생성()` | 독스 템플릿 → PDF → 드라이브 → 메일 | → PDF URL | 월요일 트리거 / 수동 |
| `트리거설정()` | 기존 트리거 삭제 후 2개 재생성 | | 최초 1회 수동 |
| `가상데이터생성(일수)` | 4주치 합성 응답을 `원본응답`에 기록 | | 개발 중 1회 |
| `전체재판정()` | 원본 전행 재판정(기준값 변경 후) | | 수동 |

핵심 스켈레톤:

```javascript
function 이상치판정(응답, 기준목록) {
  const 결과 = [];
  기준목록.filter(k => k.사용여부).forEach(k => {
    const v = 응답[k.컬럼명];
    let 이상 = false;
    if (k.유형 === '초과') 이상 = Number(v) > Number(k.기준);
    else if (k.유형 === '미만') 이상 = Number(v) < Number(k.기준);
    else if (k.유형 === '값일치') 이상 = k.이상값.split(',').map(s=>s.trim()).includes(String(v));
    if (이상) 결과.push({항목명:k.항목명, 측정값:v, 기준:k.기준||k.이상값, 심각도:k.심각도, 메일:k.메일});
  });
  return 결과;
}

function onFormSubmit(e) {
  const 응답 = {}; Object.keys(e.namedValues).forEach(k => 응답[k] = e.namedValues[k][0]);
  const 이상 = 이상치판정(응답, 기준값읽기());
  if (이상.length) { 이상이력기록(응답, 이상); 메일알림(응답, 이상); }
  원본응답판정기록(e.range.getRow(), 이상);   // N열 '정상'/'이상', O열 건수
  일일집계갱신(new Date());
}
```

---

## 6. 트리거 설정 절차 (클릭만으로)

1. 시트 상단 메뉴 **확장 프로그램 → Apps Script**
2. 왼쪽 **시계 아이콘(트리거)** → 오른쪽 아래 **+ 트리거 추가**
3. **폼 제출 트리거**: 함수 `onFormSubmit` / 배포 `Head` / 이벤트 소스 **스프레드시트에서** / 이벤트 유형 **양식 제출 시** / 오류 알림 **즉시 알림** → 저장 → 권한 허용(경고 화면: "고급 → 프로젝트로 이동(안전하지 않음)" → 허용)
4. **주간 트리거**: 함수 `주간PDF생성` / 이벤트 소스 **시간 기반** / **주 단위 타이머** / 요일 **월요일** / 시간 **오전 7시~8시** → 저장
5. 검증: 폼 테스트 제출 → **실행** 메뉴에서 `onFormSubmit` 상태 "완료" 확인

---

## 7. 주간 PDF 생성 방식

1. 구글독스로 `주간보고_템플릿` 작성. 자리표시자: `{{주차}}` `{{기간}}` `{{총제출}}` `{{이상건수}}` `{{이상률}}` `{{TOP3설비}}` `{{미조치}}` `{{이상표}}` `{{생성일시}}`. 폰트는 **Noto Sans KR** 전체 지정.
2. 문서 ID(URL의 `/d/`와 `/edit` 사이)와 저장 폴더 ID를 `설정` 탭에 기입.
3. 스크립트 흐름:

```javascript
function 주간PDF생성() {
  const 요약 = 주간요약갱신(지난주월요일());
  const 사본 = DriveApp.getFileById(설정('템플릿문서ID')).makeCopy('임시_' + 요약.주차);
  const doc = DocumentApp.openById(사본.getId()); const body = doc.getBody();
  Object.entries(요약).forEach(([k, v]) => body.replaceText('{{' + k + '}}', String(v)));
  const p = body.findText('{{이상표}}').getElement().getParent();
  body.insertTable(body.getChildIndex(p), 이상표데이터(요약.시작일)); p.removeFromParent();
  doc.saveAndClose();
  const pdf = DriveApp.getFileById(사본.getId()).getAs('application/pdf')
              .setName('주간설비점검보고_' + 요약.주차 + '.pdf');
  const 파일 = DriveApp.getFolderById(설정('PDF폴더ID')).createFile(pdf);
  사본.setTrashed(true);
  GmailApp.sendEmail(설정('관리자이메일'), '[주간보고] ' + 요약.주차, '첨부 확인 바랍니다.', {attachments:[pdf]});
  주간요약PDF링크기록(요약.주차, 파일.getUrl());
}
```

---

## 8. 조건부 서식 규칙

`원본응답` 탭, 서식 → 조건부 서식 → 맞춤 수식:

| 대상 범위 | 색 | 맞춤 수식 |
|---|---|---|
| `D2:D` 토출압력 | 빨강 `#f4cccc` | `=D2>VLOOKUP("토출압력",기준값!$A:$D,4,FALSE)` |
| `G2:G` 베어링온도 | 빨강 | `=G2>VLOOKUP("베어링온도",기준값!$A:$D,4,FALSE)` |
| `H2:H` 탱크액위 | 주황 `#fce5cd` | `=H2<VLOOKUP("탱크액위",기준값!$A:$D,4,FALSE)` |
| `E2:E,I2:M` 선택형 | 빨강 | `=OR(E2="보충필요",E2="이상",E2="있음",E2="해제됨",E2="파손",E2="미실시")` |
| `F2:F` 진동 | 노랑 `#fff2cc` | `=F2="주의"` |
| `N2:N` 판정결과 | 빨강 글자 굵게 | `=N2="이상"` |
| `이상이력!J2:J` 조치상태 | 초록 `#d9ead3` / 회색 | `=J2="완료"` / `=J2=""` |

---

## 9. Claude와 대화하며 만드는 시퀀스

| # | 단계 | Claude에게 이렇게 물어보세요 |
|---|---|---|
| 1 | 설계 고정 | "구글시트 탭이 `원본응답`, `기준값`, `이상이력`, `일일집계`, `주간요약`, `설정`이고 헤더는 [붙여넣기]야. 이 구조를 기억하고 앞으로 코드는 전부 이 헤더 이름으로 접근하게 해줘. 하드코딩 열번호 금지." |
| 2 | 기준값 읽기 | "`기준값` 탭을 `[{항목명, 컬럼명, 유형, 기준, 이상값, 심각도, 메일, 사용여부}]` 배열로 돌려주는 `기준값읽기()`를 Apps Script로 만들어줘. 헤더 1행, 사용여부는 TRUE/FALSE 문자열도 처리." |
| 3 | 판정 | "`이상치판정(응답, 기준목록)`을 만들어줘. 유형은 초과/미만/값일치 3가지, 값일치는 쉼표로 여러 값. 빈 값은 정상 처리. 테스트용 `테스트_판정()`도 같이." |
| 4 | 폼 트리거 | "`onFormSubmit(e)`에서 `e.namedValues`를 객체로 바꾸고 판정 → 이상이력 기록 → N,O열 기입까지 연결해줘. `e.range.getRow()`로 방금 행을 찾아." |
| 5 | 오류 붙여넣기 | "실행 로그에 이 오류가 나와. 원인과 고친 전체 함수를 줘: `TypeError: Cannot read properties of undefined (reading 'namedValues')`" (→ 편집기에서 직접 실행하면 나는 오류. 폼 제출로 테스트) |
| 6 | 메일 | "`메일알림`은 담당자이메일별로 이상항목을 묶어 1통씩 보내고, 제목은 `[설비이상] PU-03 진동소음 외 1건`, 본문은 표 형태 HTML로. `설정!알림활성화`가 FALSE면 보내지 마." |
| 7 | 집계 | "`일일집계갱신(날짜)`: 그 날짜 행이 있으면 덮어쓰고 없으면 추가. 미점검설비수는 `설비목록` 20대 중 제출 안 된 설비 수야." |
| 8 | PDF | "구글독스 템플릿 ID를 `설정` 탭에서 읽어 사본 만들고 `{{키}}` 치환 후 `{{이상표}}` 자리에 표를 삽입하고 PDF 저장·메일 첨부하는 `주간PDF생성()` 전체 코드 줘. 임시 사본은 휴지통으로." |
| 9 | 권한 오류 | "트리거 실행 시 `Exception: You do not have permission to call DriveApp.getFileById`가 나와. appsscript.json에 oauthScopes 어떻게 넣어야 해?" |
| 10 | 시간대 | "타임스탬프가 9시간 어긋나. 스크립트 시간대와 `Utilities.formatDate`를 Asia/Seoul로 맞추는 방법 알려줘." |
| 11 | 리팩터 | "지금 코드 전체를 붙여넣을게. 중복 제거하고 함수마다 한국어 한 줄 주석 달아줘. 동작은 바꾸지 마." |
| 12 | 설명 준비 | "이 코드를 비개발자인 내가 면접관에게 3분 안에 설명해야 해. 함수 흐름을 5문장으로 요약해줘." |

**막혔을 때 규칙:** 오류문 전체 + 해당 함수 전체 + "어느 줄에서 났는지"를 한 번에 붙여넣기. 조각만 주면 헛돈다.

---

## 10. 가상 데이터 4주치 만들기

설계: 28일 × 20대 = 560건. 주말 포함 매일 5명이 4대씩. 이상 10% ≈ 56건. 특정 설비(`PU-03`, `AC-02`)에 이상을 몰아 "TOP3 설비"가 그럴듯하게.

Claude 프롬프트:

> "Apps Script 함수 `가상데이터생성(일수)`를 만들어줘. 오늘 기준 과거 28일간, 매일 `설비목록` 20대 전부에 대해 `원본응답` 헤더 순서대로 행을 만들어. 점검자 5명 순환 배정. 정상값 범위: 토출압력 5.5~6.8, 베어링온도 45~65, 탱크액위 40~85. 전체의 10%만 이상(압력 7.1~8.5 / 온도 71~90 / 액위 5~19 / 선택형은 이상값)으로 만들고, PU-03과 AC-02는 이상 확률 3배. 타임스탬프는 그날 08:00~09:30 사이 랜덤. 특이사항은 이상 행에만 '소음 증가 확인' 같은 짧은 문장 20종 중 하나. 마지막에 `전체재판정()`을 호출해."

**주의:** 생성 함수는 폼 트리거를 타지 않으므로 반드시 `전체재판정()`으로 이상이력·N열을 채운다. 생성 후 실수로 못 누르게 함수명 앞에 `_dev_`를 붙인다.

---

## 11. 7일 실행 계획

| 일차 | 오전 | 오후 | 완료 기준 |
|---|---|---|---|
| 1 | **첫 30분**: ① 새 구글 계정(또는 기존) 폴더 `점검시스템_포트폴리오` 생성 ② 폼 새로 만들기 → 문항 1~12 입력(20분) ③ 응답 → 시트 연결, 탭 이름 `원본응답` ④ 스마트폰으로 1건 제출. 이후 나머지 탭 6개 생성·헤더 입력 | `기준값`·`설비목록`·`설정` 데이터 입력, 조건부 서식 8줄 적용 | 폰에서 제출한 값이 시트에 뜨고, 7.5 입력하면 빨갛게 됨 |
| 2 | 프롬프트 1~3으로 `기준값읽기`·`이상치판정` 작성, `테스트_판정` 로그 확인 | 프롬프트 4로 `onFormSubmit` 연결, 폼 트리거 설정 | 폼 제출 → `이상이력` 행 추가, N열 "이상" |
| 3 | 프롬프트 6 메일알림, 본인 메일 수신 테스트 | 프롬프트 7 일일집계 | 이상 제출 1분 내 메일 도착, `일일집계` 1행 갱신 |
| 4 | 가상데이터 560건 생성 + 전체재판정 | 구글독스 템플릿 작성, 문서ID·폴더ID 설정 탭 기입 | `이상이력` 50건 내외, 템플릿에 자리표시자 9개 |
| 5 | 프롬프트 8 `주간PDF생성` 작성·수동 실행 | 주간 트리거 설정, 시간대·폰트 확인, 프롬프트 11 리팩터 | PDF 저장 + 메일 첨부 도착, 한글 안 깨짐 |
| 6 | 검증 체크리스트 전 구간 2회 | 시연 영상 촬영, README 작성 | 30초 영상 1개, README 완성 |
| 7 | 노션 요약, 면접 답변 연습 | 예비: 밀린 것 처리, 백업 사본 | 남에게 3분 설명 가능 |

---

## 12. 일정 압박 시 컷 순서 (4일차 기준)

| 우선 | 항목 | 판단 |
|---|---|---|
| **지킨다** | 폼→시트→판정→색상→이상이력 | 없으면 시스템이 아님 |
| **지킨다** | 메일 알림 | 시연 임팩트 최대 |
| **지킨다** | 기준값 탭 외부화 | 면접 방어의 핵심 |
| 축소 | 주간 PDF → 표 삽입 빼고 텍스트 치환만 | 표는 "TOP3설비: PU-03(6건)…" 한 줄로 |
| 축소 | 가상데이터 4주 → 1주 140건 | 주간요약 1행이면 충분 |
| 버린다 | 일일집계 미점검설비수, 조치상태 관리 | README에 "다음 단계"로 |
| 버린다 | 리팩터·주석 정리 | 동작이 우선 |

---

## 13. 검증 체크리스트 (면접 전)

- [ ] 폰 폼 제출 → 10초 내 `원본응답` 행 추가
- [ ] 압력 7.5 입력 → D열 빨강, N열 "이상", `이상이력` 1행
- [ ] 압력 6.0·전항목 정상 → N열 "정상", 이상이력 추가 없음
- [ ] `기준값!D2`를 6.0으로 바꾸고 6.5 제출 → 이상 판정 (다시 7.0 복구)
- [ ] 이상 제출 → 담당자 메일 2분 내 수신, 제목·설비명 정확
- [ ] `설정!알림활성화=FALSE` → 메일 안 감 (복구)
- [ ] `주간PDF생성` 수동 실행 → PDF 생성, 한글·표 정상, 메일 첨부 열림
- [ ] 트리거 2개 활성, 실행 기록에 실패 없음
- [ ] 다른 기기(면접장 노트북 가정)에서 로그인 가능
- [ ] 시트·스크립트 사본 백업(`파일 → 사본 만들기`)
- [ ] 실제 회사명·설비명·인명 검색: `Ctrl+F`로 회사명 0건

---

## 14. 시연 30초 영상 스토리보드

| 장면 | 초 | 화면 | 자막 |
|---|---|---|---|
| 1 | 0–5 | 폰 폼에서 `PU-03` 선택, 진동 "이상" 선택, 제출 | "현장에서 폰으로 점검값 입력" |
| 2 | 5–11 | 시트 행 추가되며 셀 빨갛게 | "제출 즉시 시트 반영, 기준 초과 자동 표시" |
| 3 | 11–17 | Gmail 알림 도착, 제목 `[설비이상] PU-03 진동소음` | "담당자에게 1분 내 메일" |
| 4 | 17–24 | `기준값` 탭 7.0→6.0 수정 후 재제출, 판정 바뀜 | "기준은 코드 아닌 시트에서 관리" |
| 5 | 24–30 | 드라이브 PDF 열어 주간보고 표 | "매주 월요일 PDF 자동 발송" |

촬영: 폰 화면 녹화 + 노트북 화면 녹화(OBS 무료) → CapCut 무료로 편집.

---

## 15. 포트폴리오 패키징

**README.md 목차**
1. 한 줄 소개 / 2. 문제 상황(현장 경험 기반) / 3. 시스템 구조도 / 4. 시연 영상·스크린샷 / 5. 주요 기능(판정·알림·PDF) / 6. 기준값 관리 방식 / 7. 내가 결정한 것 3가지 / 8. AI 활용 방식(프롬프트 로그) / 9. 한계와 다음 단계 / 10. 실행 방법(사본 만들기 → 트리거 설정) / 11. 데이터 고지(전부 합성)

**노션 1페이지 요약 4줄**
- **문제**: 종이 점검표는 이상값이 있어도 사무실에 올라와야 알 수 있었고, 주간보고는 매주 2시간을 손으로 옮겨 적었다.
- **AI 활용**: 시트 구조와 판정 기준은 내가 설계하고, Apps Script 코드는 Claude와 12단계 대화로 작성·디버깅했다.
- **결과**: 폼 제출 후 1분 내 이상 판정·메일 알림, 월요일 07시 주간 PDF 자동 발송, 기준 변경은 셀 수정만으로 가능.
- **배운 점**: 코드보다 "무엇을 이상으로 볼지"와 "누가 기준을 바꿀 수 있어야 하는지"를 정하는 게 시스템의 본체였다.

---

## 16. 면접 방어 포인트

| 내가 결정한 것 | 근거를 보여주는 방법 |
|---|---|
| 임계값을 코드가 아닌 `기준값` 탭에 둔 것 | 현장 경험: 계절·설비 노후로 기준이 바뀌는데 매번 개발자를 부를 수 없음. 시연에서 셀 수정 후 판정 변화 실연 |
| 항목별 심각도·담당자 분리(기계/운전/안전) | 실제 조직은 누설과 밸브 잠금의 담당 부서가 다름. `기준값` 탭 F,G열과 메일 분기 로직 지목 |
| "주의"도 이상으로 잡되 노랑으로 구분 | 진동은 조기 징후를 잡는 게 목적. 조건부 서식 표와, 그렇게 지시한 프롬프트 스크린샷 |

추가: Claude 대화 로그를 노션에 공개(프롬프트 3·6·9). **"AI가 짰지만, 무엇을 짜게 할지와 틀린 걸 알아본 것은 나"**로 답한다.

---

## 17. 리스크·주의

| 함정 | 대응 |
|---|---|
| Apps Script 할당량(무료: 메일 100통/일, 트리거 실행 90분/일, 1회 6분) | 메일은 담당자별 묶음 발송, 전체재판정은 개발 중에만 |
| 시간대 | `appsscript.json`에 `"timeZone":"Asia/Seoul"` + 시트 파일 설정 → 시간대 서울. 둘 다 필요(안 맞추면 월요일 트리거가 일요일에 돎) |
| 한글 인코딩 | 스크립트는 UTF-8. 한글 함수명은 동작하지만 트리거 목록에서 깨져 보임 → **트리거 대상 함수는 영문명**(`onFormSubmit`, `weeklyReport`) 권장 |
| PDF 한글 폰트 | 독스 템플릿 폰트를 Noto Sans KR로 통일. HTML→PDF 방식은 폰트 누락 위험, 사용 금지 |
| 폼 헤더 변경 | 폼 문항 제목을 바꾸면 시트 헤더가 바뀌어 `기준값!원본컬럼명`과 불일치 → 전부 정상 처리됨. 문항 수정 후 재검증 |
| 편집기에서 `onFormSubmit` 직접 실행 | e가 undefined → 오류. 반드시 폼 제출로 테스트하거나 `테스트_판정` 사용 |
| 권한 경고 | "확인되지 않은 앱" 경고는 본인 스크립트라 정상. 고급 → 이동 → 허용 |
| 회사 정보 노출 | 실제 설비 태그·도면·양식·인명·사진 금지. 메일은 `example.com`. 면접 시 "전부 합성 데이터"를 먼저 말함 |
| 시연 중 네트워크 | 오프라인 대비 영상·PDF·스크린샷을 로컬에 준비 |
| 시트 공유 링크 | "링크가 있는 모든 사용자 → 뷰어"만. 편집 권한 공개 금지 |